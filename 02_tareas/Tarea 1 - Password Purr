### 🐾 Tarea: Password Purr-fect 🐱

Esta tarea forma parte del módulo de **Fundamentos de Python** en el programa Technolochicas 2025. El objetivo es aplicar conceptos de manipulación de cadenas de texto (strings) y lógica de programación para crear un validador de contraseñas "a prueba de mininos".

#### 🎯 Objetivo del Ejercicio
Desarrollar un script en Python que evalúe si una contraseña es lo suficientemente segura (Purr-fect) o si necesita mejoras, siguiendo una serie de reglas lógicas establecidas en el video tutorial.

#### 🧠 Conceptos Aplicados
* **Input de datos:** Uso de `input()` para recibir la propuesta de contraseña.
* **Condicionales (`if`, `elif`, `else`):** Para verificar el cumplimiento de los requisitos de seguridad.
* **Métodos de String:** Uso de funciones como `.isupper()`, `.islower()`, `.isdigit()` y `len()`.
* **Operadores Lógicos:** Combinación de múltiples criterios usando `and` y `or`.

#### 📋 Requisitos de la Contraseña "Purr-fect"
Para que la contraseña sea aprobada por el "Gato Guardián", debe cumplir con:
1.  **Longitud mínima:** Al menos 8 caracteres (¡mientras más larga, más difícil de atrapar!).
2.  **Variedad de caracteres:** Debe incluir al menos una mayúscula y un número.
3.  **Carácter Especial:** Debe contener al menos un símbolo (ej. `!`, `@`, `#`, `$`).
4.  **No "Gatitos" obvios:** No debe contener la palabra "meow" o "miau" por razones de seguridad felina.

#### 🛠️ Instrucciones de Ejecución
1. Navega a la carpeta `01_fundamentos/password_purrfect`.
2. Ejecuta el archivo con:
   ```bash
   python password_validador.py
